import 'package:flutter/material.dart';

class MissionsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Missões'),
      ),
      body: Center(
        child: Text('Aqui será exibida a lista de missões.'),
      ),
    );
  }
}
